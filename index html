<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Surprise Love Letter 🦋</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      height: 100vh;
      overflow: hidden;
      background: linear-gradient(120deg, #f8cdda 0%, #1d2b64 100%);
      font-family: 'Segoe UI', cursive, sans-serif;
      position: relative;
    }
    .container {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      background: rgba(255,255,255,0.95);
      border-radius: 18px;
      padding: 2.5rem;
      max-width: 400px;
      box-shadow: 0 8px 32px rgba(0,0,0,0.17);
      text-align: center;
      z-index: 1;
    }
    .container h1 {
      color: #e75480;
      margin-bottom: 10px;
      font-size: 2em;
    }
    .container p {
      color: #7e005a;
      font-size: 1.1em;
      margin: 0.6em 0;
      letter-spacing: 0.02em;
    }
    .butterfly {
      position: absolute;
      width: 48px;
      height: 44px;
      pointer-events: none;
      opacity: 0.86;
      animation: flutter infinite linear;
    }
    @keyframes flutter {
      0% { transform: translateY(0) rotate(0deg) scale(1); }
      50% { transform: translateY(-40px) rotate(7deg) scale(1.12); }
      100% { transform: translateY(0) rotate(0deg) scale(1); }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>My Love,</h1>
    <p>
      You are the sunshine of my day, the music in my heart, and the butterflies in my stomach.
      This little surprise is just a reminder of how special you are to me. Every day with you is like a new spring, full of hope, beauty, and gentle joy.
    </p>
    <p>
      <strong>I love you—today, tomorrow, always.</strong>
    </p>
    <p style="color:#5151c6;">🦋</p>
    <p style="font-size:0.95em; color:#888;">(Refresh for more butterflies!)</p>
  </div>
  <script>
    const butterflySVG = `
      <svg viewBox="0 0 48 44" fill="none" xmlns="http://www.w3.org/2000/svg">
        <ellipse cx="12" cy="16" rx="12" ry="16" fill="#fed6f2"/>
        <ellipse cx="36" cy="16" rx="12" ry="16" fill="#c9f5cb"/>
        <ellipse cx="24" cy="39" rx="6" ry="5" fill="#ffba98"/>
        <rect x="22" y="14" width="4" height="20" rx="2" fill="#6b0049"/>
        <ellipse cx="24" cy="11" rx="5" ry="4" fill="#fa9eff"/>
        <ellipse cx="10" cy="5" rx="5" ry="2" fill="#6b0049" opacity="0.3"/>
        <ellipse cx="38" cy="5" rx="5" ry="2" fill="#6b0049" opacity="0.3"/>
      </svg>
    `;

    function createButterfly() {
      const el = document.createElement('div');
      el.className = 'butterfly';
      el.style.left = Math.random() * 98 + 'vw';
      el.style.top = Math.random() * 80 + 5 + 'vh';
      el.style.animationDuration = (4.5 + Math.random() * 2.5) + 's';
      el.style.transform = `rotate(${Math.random()*360}deg) scale(${0.85 + Math.random()*0.4})`;
      el.innerHTML = butterflySVG;
      return el;
    }

    for (let i = 0; i < 15; i++) {
      document.body.appendChild(createButterfly());
    }
  </script>
</body>
</html>
